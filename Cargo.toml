[package]
name = "agy-gyro"
version = "0.1.2"
edition = "2024"
authors = ["John Wu <topjohnwu@gmail.com>"]
description = "Local retry proxy to stabilize the Antigravity CLI (agy) against transient Google Gemini API errors"
readme = "README.md"
license = "MIT"
repository = "https://github.com/topjohnwu/agy-gyro"
homepage = "https://github.com/topjohnwu/agy-gyro"
keywords = ["gemini", "retry", "proxy", "antigravity", "llm"]
categories = [
    "command-line-utilities",
    "development-tools",
    "network-programming",
]
include = [
    "src/**/*",
    "tests/**/*",
    "assets/*",
    "Cargo.toml",
    "README.md",
    "LICENSE",
]

[dependencies]
tokio = { version = "1.53", features = ["full"] }
axum = { version = "0.8", features = ["tokio"] }
reqwest = { version = "0.13", default-features = false, features = [
    "rustls",
    "stream",
    "json",
    "http2",
] }
clap = { version = "4.6", features = ["derive", "env"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
rand = "0.10"
futures-util = { version = "0.3", default-features = false, features = [
    "alloc",
] }
httpdate = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[dev-dependencies]
wiremock = "0.6"

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
panic = "abort"
strip = true
